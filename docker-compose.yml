version: '3.8'
services:
  app:
    build: .
    ports:
      - "3000:3000"
    # Create a .env.local file in the root directory
    # with your Firebase credentials before running docker-compose
    env_file:
      - .env.local
